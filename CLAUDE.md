# CLAUDE.md

Full project instructions, context, and copy manual are in the private internal submodule.

@internal/CLAUDE.md

---

## Quick reference (public)

- Dev server: `npm run dev`
- Build: `npm run build`
- Internal submodule: `internal/` → `BeardlessDeveloper/a1-website-internal` (private)
- If submodule is empty: `git submodule update --init`
